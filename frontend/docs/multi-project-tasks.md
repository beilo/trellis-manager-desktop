# 多项目管理功能实施任务

## Phase 1: 数据层

- [ ] **1.1** 扩展后端 ManagerConfig，新增 `projects` 和 `last_selected_project` 字段
- [ ] **1.2** 实现 `get_projects()` API，返回已保存的项目列表
- [ ] **1.3** 实现 `save_projects()` API，保存项目列表
- [ ] **1.4** 实现 `add_project(path)` API，带验证
- [ ] **1.5** 实现 `remove_project(path)` API
- [ ] **1.6** 数据迁移：现有 `recent_projects` 转为初始 `projects` 列表

## Phase 2: 前端类型与 API

- [ ] **2.1** 扩展 `types.ts`，新增 ManagerConfig 类型字段
- [ ] **2.2** 扩展 `api.ts`，新增前端 API 封装
- [ ] **2.3** 定义 `AppState` 新增字段类型

## Phase 3: 核心状态管理 (App.tsx)

- [ ] **3.1** 新增 `activeTab` 状态，默认为 `'projects'`
- [ ] **3.2** 新增 `projects` 状态（项目列表）
- [ ] **3.3** 新增 `selectedProject` 状态
- [ ] **3.4** 新增 `projectStatuses` 缓存状态
- [ ] **3.5** 初始化时加载项目列表和上次选中项目
- [ ] **3.6** Tab 切换逻辑
- [ ] **3.7** SummaryCards 根据 Tab 动态显示/隐藏项目卡片

## Phase 4: UI 组件

### 4.1 Header Tab 切换
- [ ] **4.1.1** Header 组件添加 Tab 按钮
- [ ] **4.1.2** Tab 切换交互样式

### 4.2 ProjectList 组件（新建）
- [ ] **4.2.1** 项目列表渲染（名称 + 状态指示器）
- [ ] **4.2.2** 选中状态高亮
- [ ] **4.2.3** hover 显示删除按钮
- [ ] **4.2.4** "+ 添加项目" 按钮
- [ ] **4.2.5** 空列表提示
- [ ] **4.2.6** 滚动容器样式

### 4.3 项目 Tab 布局
- [ ] **4.3.1** 侧边栏 + 主区域双栏布局
- [ ] **4.3.2** 选中项目详情区域
- [ ] **4.3.3** 工具链 Tab 全宽布局

### 4.4 SummaryCards 联动
- [ ] **4.4.1** 工具链 Tab 只显示 3 个工具链卡片
- [ ] **4.4.2** 项目 Tab 显示全部 4 个卡片
- [ ] **4.4.3** "当前项目" 跟随选中项目更新

## Phase 5: 业务逻辑

- [ ] **5.1** 添加项目：选择目录 → 检查 → 保存 → 选中
- [ ] **5.2** 删除项目：从列表移除 → 保存
- [ ] **5.3** 选中项目：更新选中状态 → 加载详情 → 保存选中
- [ ] **5.4** Init/Update 操作更新项目状态缓存
- [ ] **5.5** 切换 Tab 保持状态

## Phase 6: 边界处理

- [ ] **6.1** 项目路径不存在处理
- [ ] **6.2** 选中项目被删除后自动切换
- [ ] **6.3** 空项目列表引导提示

## Phase 7: 测试与文档

- [ ] **7.1** 手动测试全流程
- [ ] **7.2** 更新 README 或内嵌文档

---

## 任务执行顺序

```
Phase 1 (后端) → Phase 2 (前端类型) → Phase 3 (状态) → Phase 4 (UI) → Phase 5 (逻辑) → Phase 6 (边界) → Phase 7 (收尾)
```

建议分批次实现：
1. 第一批：Phase 1 + 2（后端 + API）
2. 第二批：Phase 3 + 4 + 5（前端核心）
3. 第三批：Phase 6 + 7（边界 + 测试）
